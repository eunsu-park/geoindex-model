# Experiment: gnn_bilstm for in3d out12h
# Usage: python scripts/train.py --config-name=in3d_out12h_gnn_bilstm

defaults:
  - in3d_out12h
  - _self_

experiment:
  name: "in3d_out12h_gnn_bilstm"

model:
  model_type: "gnn"
  gnn_temporal_type: "bilstm"
