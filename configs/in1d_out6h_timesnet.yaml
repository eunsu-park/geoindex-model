# Experiment: timesnet, in1d input, out6h output
# Usage: python scripts/train.py --config-name=in1d_out6h_timesnet

defaults:
  - in1d_out6h
  - _self_

experiment:
  name: "in1d_out6h_timesnet"

model:
  model_type: "timesnet"
