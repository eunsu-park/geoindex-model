# Experiment: timesnet, in3d input, out6h output
# Usage: python scripts/train.py --config-name=in3d_out6h_timesnet

defaults:
  - in3d_out6h
  - _self_

experiment:
  name: "in3d_out6h_timesnet"

model:
  model_type: "timesnet"
