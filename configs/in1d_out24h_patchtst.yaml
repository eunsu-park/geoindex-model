# Experiment: patchtst, in1d input, out24h output
# Usage: python scripts/train.py --config-name=in1d_out24h_patchtst

defaults:
  - in1d_out24h
  - _self_

experiment:
  name: "in1d_out24h_patchtst"

model:
  model_type: "patchtst"
