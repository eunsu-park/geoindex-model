# Experiment C2: A5 (low LR) + B2 (Gaussian noise augmentation)
# Goal: Combine best overfitting fix with data augmentation
# Usage: python scripts/train.py --config-name=in2d_out24h_C2

defaults:
  - in2d_out24h_A5
  - _self_

experiment:
  name: "in2d_out24h_C2_lowlr_augment"

data:
  timeseries:
    augmentation:
      gaussian_noise_std: 0.05
