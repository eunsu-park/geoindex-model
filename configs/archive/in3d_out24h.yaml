# Experiment: 3-day input, 24-hour output
# Usage: python scripts/train.py --config-name=in3d_out24h

defaults:
  - local
  - _self_

experiment:
  name: "in3d_out24h"

data:
  timeseries:
    input_start: -144
    input_end: 0
    target_start: 0
    target_end: 48

model:
  model_type: "linear"
