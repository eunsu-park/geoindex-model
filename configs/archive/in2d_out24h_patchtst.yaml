# Experiment: patchtst, in2d input, out24h output
# Usage: python scripts/train.py --config-name=in2d_out24h_patchtst

defaults:
  - in2d_out24h
  - _self_

experiment:
  name: "in2d_out24h_patchtst"

model:
  model_type: "patchtst"
