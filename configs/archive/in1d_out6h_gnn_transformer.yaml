# Experiment: gnn_transformer, in1d input, out6h output
# Usage: python scripts/train.py --config-name=in1d_out6h_gnn_transformer

defaults:
  - in1d_out6h
  - _self_

experiment:
  name: "in1d_out6h_gnn_transformer"

model:
  model_type: "gnn"
  gnn_temporal_type: "transformer"
