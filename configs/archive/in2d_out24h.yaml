# Experiment: 2-day input, 24-hour output
# Usage: python scripts/train.py --config-name=in2d_out24h

defaults:
  - local
  - _self_

experiment:
  name: "in2d_out24h"

data:
  timeseries:
    input_start: -96
    input_end: 0
    target_start: 0
    target_end: 48

model:
  model_type: "linear"
