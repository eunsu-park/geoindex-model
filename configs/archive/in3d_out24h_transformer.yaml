# Experiment: transformer, in3d input, out24h output
# Usage: python scripts/train.py --config-name=in3d_out24h_transformer

defaults:
  - in3d_out24h
  - _self_

experiment:
  name: "in3d_out24h_transformer"

model:
  model_type: "transformer"
