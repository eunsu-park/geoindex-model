# Experiment: patchtst, in1d input, out6h output
# Usage: python scripts/train.py --config-name=in1d_out6h_patchtst

defaults:
  - in1d_out6h
  - _self_

experiment:
  name: "in1d_out6h_patchtst"

model:
  model_type: "patchtst"
