# Experiment: gnn_patchtst, in3d input, out6h output
# Usage: python scripts/train.py --config-name=in3d_out6h_gnn_patchtst

defaults:
  - in3d_out6h
  - _self_

experiment:
  name: "in3d_out6h_gnn_patchtst"

model:
  model_type: "gnn"
  gnn_temporal_type: "patch_transformer"
