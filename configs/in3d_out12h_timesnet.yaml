# Experiment: timesnet for in3d out12h
# Usage: python scripts/train.py --config-name=in3d_out12h_timesnet

defaults:
  - in3d_out12h
  - _self_

experiment:
  name: "in3d_out12h_timesnet"

model:
  model_type: "timesnet"
