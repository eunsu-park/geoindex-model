# Experiment: TimesNet encoder for 12h prediction
# Usage: python scripts/train.py --config-name=in2d_out12h_timesnet

defaults:
  - in2d_out12h
  - _self_

experiment:
  name: "in2d_out12h_timesnet"

model:
  model_type: "timesnet"
