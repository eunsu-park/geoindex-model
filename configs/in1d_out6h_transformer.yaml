# Experiment: transformer, in1d input, out6h output
# Usage: python scripts/train.py --config-name=in1d_out6h_transformer

defaults:
  - in1d_out6h
  - _self_

experiment:
  name: "in1d_out6h_transformer"

model:
  model_type: "transformer"
