# Experiment: tcn, in2d input, out24h output
# Usage: python scripts/train.py --config-name=in2d_out24h_tcn

defaults:
  - in2d_out24h
  - _self_

experiment:
  name: "in2d_out24h_tcn"

model:
  model_type: "tcn"
