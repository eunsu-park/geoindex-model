# Experiment: gnn_bilstm, in3d input, out6h output
# Usage: python scripts/train.py --config-name=in3d_out6h_gnn_bilstm

defaults:
  - in3d_out6h
  - _self_

experiment:
  name: "in3d_out6h_gnn_bilstm"

model:
  model_type: "gnn"
  gnn_temporal_type: "bilstm"
