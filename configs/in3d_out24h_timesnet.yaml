# Experiment: timesnet, in3d input, out24h output
# Usage: python scripts/train.py --config-name=in3d_out24h_timesnet

defaults:
  - in3d_out24h
  - _self_

experiment:
  name: "in3d_out24h_timesnet"

model:
  model_type: "timesnet"
