# Experiment: transformer, in3d input, out12h output
# Usage: python scripts/train.py --config-name=in3d_out12h_transformer

defaults:
  - in3d_out12h
  - _self_

experiment:
  name: "in3d_out12h_transformer"

model:
  model_type: "transformer"
