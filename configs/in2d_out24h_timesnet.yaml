# Experiment: timesnet, in2d input, out24h output
# Usage: python scripts/train.py --config-name=in2d_out24h_timesnet

defaults:
  - in2d_out24h
  - _self_

experiment:
  name: "in2d_out24h_timesnet"

model:
  model_type: "timesnet"
