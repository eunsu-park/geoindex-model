# Experiment: transformer, in1d input, out12h output
# Usage: python scripts/train.py --config-name=in1d_out12h_transformer

defaults:
  - in1d_out12h
  - _self_

experiment:
  name: "in1d_out12h_transformer"

model:
  model_type: "transformer"
