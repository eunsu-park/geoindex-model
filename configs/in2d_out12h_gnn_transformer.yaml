# Experiment: GNN + Transformer temporal encoder, 12h prediction
# Usage: python scripts/train.py --config-name=in2d_out12h_gnn_transformer

defaults:
  - in2d_out12h
  - _self_

experiment:
  name: "in2d_out12h_gnn_transformer"

model:
  model_type: "gnn"
  gnn_temporal_type: "transformer"
