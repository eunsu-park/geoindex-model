# Experiment: transformer, in2d input, out12h output
# Usage: python scripts/train.py --config-name=in2d_out12h_transformer

defaults:
  - in2d_out12h
  - _self_

experiment:
  name: "in2d_out12h_transformer"

model:
  model_type: "transformer"
